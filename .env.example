# Environment variables seed initial config only.
# Runtime values live in the app_config table (SQLite) and are changed through
# the settings panel without restart. See AGENTS.md.
LLM_ENDPOINT=https://ollama.com/v1
LLM_MODEL=qwen3:32b
LLM_API_KEY=
PORT=3001
DB_PATH=./kumite.db
AGENT_CACHE_TTL_HOURS=24
GITHUB_RAW_BASE=https://raw.githubusercontent.com/msitarzewski/agency-agents/main
LLM_MOCK=false